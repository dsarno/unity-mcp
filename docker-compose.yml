version: "3.9"

services:
  unity-mcp-server:
    build:
      context: .
      dockerfile: Server/Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
    command: ["uv", "run", "server.py", "--transport", "http", "--http-host", "0.0.0.0", "--http-port", "8080"]
